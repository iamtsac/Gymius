<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gymius">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Gymius">
        <activity android:name=".EditSessionActivity"></activity>
        <activity android:name=".DeleteSessionActivity" />
        <activity android:name=".CreateSessionActivity" />
        <activity android:name=".ActivityDBInitializer">
            <intent-filter>
                REMOVE COMMENT WHEN YOU NEED TO INIT THE DB
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ActivityTrainer" />
        <activity android:name=".ActivityClient" />
        <activity android:name=".ActivityAdmin" />
        <activity android:name=".ActivityCreateSession" />
        <activity android:name=".ActivityGymSession" />
        <activity
            android:name=".ActivityLogin"
            android:label="@string/app_name">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>
    </application>

</manifest>